'''
'''

# Inicializar una lista de 5 elementos con valores 0

mi_lista = [0] * 5

# Ciclo for para asignar a cada posicion de la lista el valor de su indice
for i in range(len(mi_lista)):
    mi_lista[i] = int(input('Ingrese un numero: '))
